/// <summary>
/// Defines the roles that users can have in the system.
/// </summary>
public enum UserRole {
    /// <summary>
    /// Users with the Admin role have full administrative privileges.
    /// </summary>
    Admin,
    /// <summary>
    /// Standard users can access the system's features but do not have administrative privileges.
    /// </summary>
    User
}
